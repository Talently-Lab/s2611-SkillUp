# 💻 Frontend — SkillUp Campus

Aplicación web desarrollada con **React + Vite**.

## Estructura principal
* `src/`: Código fuente de la interfaz (componentes, vistas, estilos y consumo de API).
* `index.html`: Punto de entrada del DOM de la SPA (Single Page Application).
