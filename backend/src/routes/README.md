# 🛣️ Rutas de la API (routes)

Definición de los endpoints REST:
* Mapeo de métodos HTTP (`GET`, `POST`, `PUT`, `DELETE`) hacia sus correspondientes middlewares y controladores.
* División por recursos (`auth.routes.js`, `courses.routes.js`, `enrollments.routes.js`, `users.routes.js`).
